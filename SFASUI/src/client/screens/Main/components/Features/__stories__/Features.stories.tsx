import React from 'react';

import { Features } from '../Features';

export default {
    title: 'Main/Features',
    component: Features,
};

export const playground = () => {
    return <Features />;
};
