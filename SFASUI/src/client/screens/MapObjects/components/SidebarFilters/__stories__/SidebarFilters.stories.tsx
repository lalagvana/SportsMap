import React from 'react';

import { SidebarFilters } from '..';

export default {
    title: 'Map/SidebarFilters',
    component: SidebarFilters,
};

export const playground = () => (
    <div style={{ width: '100%' }}>
        <SidebarFilters />
    </div>
);
