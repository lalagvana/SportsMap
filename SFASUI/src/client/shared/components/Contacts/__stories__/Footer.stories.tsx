import React from 'react';

import { Contacts } from '../Contacts';

export default {
    title: 'Shared/Contacts',
    component: Contacts,
};

export const playground = () => {
    return <Contacts />;
};
