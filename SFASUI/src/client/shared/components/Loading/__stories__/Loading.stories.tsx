import React from 'react';

import { Loading } from '../Loading';

export default {
    title: 'Shared/Loading',
    component: Loading,
};

export const playground = () => {
    return <Loading />;
};
